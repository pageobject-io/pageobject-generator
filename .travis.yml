dist: trusty
sudo: false

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script:
  - jdk_switcher use oraclejdk8
  - mvn test
  - cd src/test/test-app
  - npm run lite

after_success:
  - bash <(curl -s https://codecov.io/bash)